console.error(
  "Direct bulk deactivation is disabled. Sign in to the admin site and use Bulk Deactivate, which requires an exact phone/card match and preserves the audit record."
);
process.exitCode = 1;
